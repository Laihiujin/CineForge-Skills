# Contributing

Contributions should improve production reliability, not merely add prompt adjectives. New skills must define: trigger conditions, inputs, outputs, hard gates, workflow, failure modes, and acceptance criteria. Keep skills model-agnostic unless placed under a clearly isolated adapter directory. Run `python3 scripts/validate_skills.py` before submitting changes.
